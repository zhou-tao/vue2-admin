import { ThemeEnum } from '@/enum'

const state = {
  title: 'vue2-starter',
  showProgress: true,
  theme: ThemeEnum.LIGHT
}

const mutations = {}

const actions = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}